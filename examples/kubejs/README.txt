Example of KubeJS traits

To add a trait:
1. add item texture
2. register trait in startup script
3. register item in startup script
4. add item to tag in server script
5. if the trait is potion trait, add it to tag in server script
6. add json for trait config
7. add description lang if the trait is a basic trait