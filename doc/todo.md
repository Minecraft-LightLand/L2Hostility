# Todo
- [ ] Entity config healthScale and attackScale being mislabeled as hp and atk
- [ ] scale, healthScale, attackScale should be edited as floating point but displayed as percentage
- [ ] Fix structure config editor crash caused by absent structure registry on client. Fix it by fetching integrated server registry access. If failed to find server, disable the structure config edit button.